// 画面サイズ用変数
var size;

// セレクト画面用変数 -----------------
var stage1_rank;
var stafe2_rank;
var stage3_rank;
var stage2_on = false;
var stage3_on = false;
// ---------------------------------

// ステージ使用用変数 ----------------
// スクロールバックグラウンド用変数
var sc_back;

// 各フラグ
var touch_count;
var pl_touching = false;
var start_ball = false;
var pl_atk = false;
var misail = false;
var doubl_tap = false;
var double_count;

// ボール関係
var ball;
var ball_sprite;
var ball_type;
var ball_spd_x;
var ball_spd_y;
var ball_layer;

// プレイヤー関係
var player_sprite;
var reflection_sprite;

var misairu_layer;

// エネミー関係
var enemys_layer;
var enemy;

var enemy_shot_layer;

// アイテム関係
var item_sprite;
var item_layer;

// 残機用変数
var life;
var lifelabel;

// 残り時間用変数
var timer;
var timelabel;
// --------------------------------

// ラウンドリザルト用変数 -------------
var resalt_timer;
var resalt_life;
var resalt_dm;
var resalt_rank;

// --------------------------------

// ステージリザルト用変数 -------------
var stage_resalt;
var stage_rank;

// --------------------------------
